[package]
name         = "psx-telemetry"
version      = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description  = "Shared guest/host telemetry id tables: stage, task and counter ids plus slot counts."

[lints.rust]
missing_docs = "warn"
