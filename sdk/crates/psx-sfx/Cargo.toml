[package]
name         = "psx-sfx"
version      = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description  = "One-shot sample playback on the PS1 SPU: a sequential sample bank, correct key-on, round-robin voices, and the length-based cutoff a one-shot needs on real hardware."

[dependencies]
psx-spu   = { path = "../psx-spu" }
psx-asset = { path = "../psx-asset" }

[lints.rust]
missing_docs = "warn"
